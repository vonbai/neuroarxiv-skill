# Keep reasoning and capability discovery with the caller

NeuroArxiv will not embed the Claude Agent SDK or probe the caller's machine for Skills, CLIs, MCP servers, or connectors. The caller owns reasoning and available tools; NeuroArxiv defines source-specific evidence obligations and requires Evidence Coverage without maintaining a runtime Capability Manifest.
